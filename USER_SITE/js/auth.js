import {auth,db} from "./firebase.js";import {createUserWithEmailAndPassword,signInWithEmailAndPassword} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {doc,setDoc,serverTimestamp} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js";const ADMIN="kundusudip019@gmail.com",$=id=>document.getElementById(id);$("loginForm")?.addEventListener("submit",async e=>{e.preventDefault();try{const email=$("email").value.trim().toLowerCase();await signInWithEmailAndPassword(auth,email,$("password").value);location.href="dashboard.html"}catch(x){$("msg").textContent=x.message}});$("registerForm")?.addEventListener("submit",async e=>{e.preventDefault();try{const name=$("name").value.trim(),email=$("email").value.trim().toLowerCase();if(email===ADMIN)throw Error("Admin email is reserved.");const c=await createUserWithEmailAndPassword(auth,email,$("password").value);const code=email.split("@")[0].slice(0,5).toUpperCase()+Math.random().toString(36).slice(2,6).toUpperCase();const ref=new URLSearchParams(location.search).get("ref")||$("referral").value.trim();await setDoc(doc(db,"users",c.user.uid),{uid:c.user.uid,name,email,balance:10000,active:true,referralCode:code,referredBy:ref,referralCount:0,createdAt:serverTimestamp()});location.href="dashboard.html"}catch(x){$("msg").textContent=x.message}});